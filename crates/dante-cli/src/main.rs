//! `dante` — headless DaNTe client for development and integration tests.
//!
//! Drives [`dante_core`] without a GUI: create/load an identity, add and verify
//! contacts, send/receive DMs and files, run relay-backed offline delivery.
//! Used by the Phase 4 integration harness described in `../../docs/DESIGN.md`.

fn main() {
    eprintln!("dante-cli: not yet implemented (Phase 4). See docs/DESIGN.md.");
    std::process::exit(1);
}
