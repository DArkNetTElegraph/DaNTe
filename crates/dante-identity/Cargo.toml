[package]
name = "dante-identity"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Identity keypair lifecycle, fingerprints, encrypted keystore, liveness proofs, key backup."

[dependencies]
dante-crypto = { workspace = true, features = ["serde"] }
thiserror.workspace = true
zeroize.workspace = true
serde = { workspace = true }
serde-big-array = "0.5"
ciborium = "0.2"
bip39 = "2"

[dev-dependencies]
hex-literal = "0.4"

[lints.rust]
unsafe_code = "forbid"
